# Advanced-Multiprocessor-Programming
We are asked to give three different, concurrent implementations of an unbounded FIFO queue data structure implemented as a linked list with a  circulating sentinel (head). Correct memory management by thread local free lists is of importance (no ABA problem). The implementations have to be carefully benchmarked against each other.
